Each module handles one endpoint type:

fixtures.py → all matches, past and future

teams.py → team info

players.py → squads + player stats

standings.py → league table

h2h.py → head-to-head for each fixture

stats.py → match statistics for each fixture